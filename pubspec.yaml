name: restbase
description: A rest client base to make API requests.
version: 0.3.0
homepage: https://github.com/iatecbr/flutter_rest

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^3.0.9
  http_parser: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: